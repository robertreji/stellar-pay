import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // allowedDevOrigins is only needed for local dev and is set via
  // NEXT_PUBLIC_ALLOWED_DEV_ORIGINS or the --hostname flag.
};

export default nextConfig;

